name: Build and Package

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [macos-latest, windows-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4 # También puedes actualizar actions/checkout a la última versión

      - name: Set up Node.js
        uses: actions/setup-node@v4 # Actualiza actions/setup-node a la última versión
        with:
          node-version: "16"

      - name: Install dependencies
        run: npm install

      - name: Build and package
        run: npm run make

      - name: Upload artifacts
        uses: actions/upload-artifact@v4 # Actualiza a la versión v4
        with:
          name: installer-${{ matrix.os }}
          path: out/make/*
